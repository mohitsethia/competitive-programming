#include<iostream>
using namespace std;
int main(){
    int t;
    cin >> t;
    while(t--){
        int n, q;
        cin >> n;
        int x[n-1], y[n-1], a[n];
        for(int i = 0; i < n-1; i++){
            cin >> x[i] >> y[i];
        }
        for(int i = 0; i < n; i++){
            cin >> a[i];
        }
        cin >> q;
        for(int i = 0; i < q; i++){
            int u, v;
            cin >> u >> v;
            
        }
    }
    return 0;
}
